# If you want to run Euler as a subsystem, it will broadcast its output on the network. This is what broadcast mode is
# Otherwise, we should be able to run Euler as a GUI that shows what each indicator is outputting, and the overall market score
broadcast_mode = True
GUI_mode = False
run_continuously = True

# The network address to broadcast on
broadcast_network = "127.0.0.1" 
broadcast_port = 5001